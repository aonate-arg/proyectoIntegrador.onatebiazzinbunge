window.addEventListener("load",function () {
    let logeado = True
    if (logeado) {
        let email = "this.document.querySelector()"
        const saludo = this.document.querySelector(".logeado")
        saludo.innerHTML = "<p>Bienvenido </p> + email"
    }
   

})